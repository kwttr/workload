@model workload_Models.ViewModels.TeacherProfileVM

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">@Model.teacher.LastName @Model.teacher.FirstName @Model.teacher.Patronymic Profile</h2>
        </div>
    </div>

    <br /><br />

        @if (Model.reportList.Count > 0)
        {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Название отчета
                    </th>
                    <th>
                        Кнопки управления
                    </th>
                </tr>
            </thead>
            <tbody>
            @foreach(var obj in Model.reportList)
                    {
                    <tr>
                        <td width="50%">@obj.Title</td>
                        <td class="text-center">
                            <div class="w-30 btn-group" role="group">
                            @if (obj.StatusId == 1)
                            {
                                <a asp-route-Id="@obj.Id" asp-action="UpdateActivities" class="btn btn-primary mx-2">Update Activities</a>
                                <a asp-route-Id="@obj.Id" asp-action="SendReport" class="btn btn-warning mx-2">Send Report</a>
                            }
                            else if(obj.StatusId == 2)
                            {
                                <a asp-route-Id="@obj.Id" asp-action="ViewReport" class="btn btn-primary mx-2">View Activities</a>
                                <button type="button" class="btn btn-dark" disabled>On review</button>
                            }
                            else if(obj.StatusId == 3)
                            {
                                <a asp-route-Id="@obj.Id" asp-action="ViewReport" class="btn btn-primary mx-2">View Activities</a>
                                <button type="button" class="btn btn-success" disabled>Approved</button>
                            }
                        </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        }
        else
        {
            <p>No reports assigned to you</p>
        }
</div>